{
  "name": "chatbridge-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test:acceptance": "playwright test --config=playwright.config.ts",
    "pw:install": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.5.0"
  }
}
